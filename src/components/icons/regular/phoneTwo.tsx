import React from 'react';
import { IconProps } from '../iconProps';

export const IconPhoneTwo: React.FC<IconProps> = ({ size, fill }: IconProps) => {
    return (
        <svg width={size} height={size}