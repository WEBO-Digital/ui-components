import { Meta, Title, Primary, Controls, Stories, Canvas } from '@storybook/blocks';
import Example from "./Example.tsx";
import * as Button from "./Button.stories.tsx";

<Meta of={Button} title="Button" />

<Title />

# Default implementation

<Primary />

## Inputs

The component accepts the following inputs (props):

<Controls />

---

## Additional variations



## Usage

```ts
import {Button} from "@webo-digital/ui-components";

const Example = () => {
  return (
      <Button
        label={"Submit"}
        size={"large"}
        disabled={false}
        buttonType={"primary"}
        leftIcon={"💚"}
        rightIcon={"🔥"}
        rounded={"lg"}
        spacing={"sm"}
        onClick={() => {}}
      />
  );
};

export default Example;
```



## Arguments

- **label** _`string`_ - A string that represents the text content of the button.
- **disabled** - A boolean indicating whether the button should be disabled or not. When disabled, the button cannot be clicked or interacted with.
- **size** - A string with one of three possible values: `default`, `small`, `large`, or `xxl`. It defines the size or dimensions of the button.
- **onClick** - A function that is called when the button is clicked. It receives a `MouseEventHandler` for handling the click event on the button element.
- **buttonType** - A string with one of three possible values: `primary`, `secondary`, `warning`, `outline`, `disabled` or `error`. It defines the type of button.
- **leftIcon** - A string that represents the left icon of the button.
- **rightIcon** - A string that represents the right icon of the button.
- **rounded** - A string with one of three possible values: `none`, `sm`, `md`, `lg`, `xl`, `full`. It defines the border radius of the button.
- **spacing** - A string with one of three possible values: `default`, `small`, `large` or `xxl`. It defines the spacing between the button text and the button icon.



## Listed below are additional variations of the component.

<Stories/>